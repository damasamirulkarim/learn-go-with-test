# Integer Add Function

## Requirements
- Create a new folder `2-integer/` following the existing numbered convention
- Implement an `Add(a, b int) int` function that returns the sum of two integers
- Include a table-driven test file `adder_test.go` covering multiple cases (positive, negative, zero)