# Iteration Module - Repeat Function

## Requirements
- Create a new chapter directory `3-iteration/`
- Implement a `Repeat(s string) string` function that returns the input string repeated 5 times
- The repeat count (5) is hardcoded, not parameterized
- Follow existing chapter conventions: `package main`, exported function, table-driven tests
- Create `3-iteration/repeat.go` with the implementation
- Create `3-iteration/repeat_test.go` with table-driven tests covering: single character, multi-character string, empty string